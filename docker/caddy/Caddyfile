# See https://caddyserver.com/docs

{
    servers {
        metrics
    }
}

http://{$DOMAIN_NAME} {
    handle {
        reverse_proxy /api/* backend:8081/api/*
        reverse_proxy /monitoring/* grafana:3000
        reverse_proxy frontend:5173
    }

	encode gzip

	log {
		output stdout
	}
}
